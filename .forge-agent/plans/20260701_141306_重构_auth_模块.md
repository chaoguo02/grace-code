Now I have a clear picture of the current codebase state. Let me synthesize what I've found.

## Current State

The `auth.py` file already matches the previously-planned refactoring (per the plan at `.forge-agent/plans/20260701_133830_重构_auth_模块.md`):
- ✅ `AuthService` class with injectable constructor
- ✅ `generate_token`, `verify_token`, `login`, `revoke_token`, `cleanup_expired`, `get_active_sessions`
- ✅ Module-level singleton + backward-compatible functions

However, there are further refactoring opportunities that would cleanly separate concerns.

---

### Goal

Continue the auth module refactoring by splitting cross-cutting concerns into the existing `middleware` package. Specifically:
- Move the `require_auth` Flask decorator out of `auth.py` into the `middleware` package
- Keep `auth.py` focused solely on the `AuthService` business logic and its backward-compatible API
- Update all importers to point to the new location

### Constraints

- Only modify files under `_test_project/`.
- The `AuthService` class and its public API must remain untouched.
- All existing endpoint behavior must remain identical.
- Keep the module-level singleton `auth_service` and backward-compat functions (`generate_token`, `verify_token`, `login`) in `auth.py` for any external callers that may depend on them.

### Steps

1. **Create `_test_project/src/middleware/require_auth.py`**  
   Extract the `require_auth` decorator (currently in `auth.py` lines 94-107) into a new file. It imports `verify_token` from `src.auth`.

2. **Update `_test_project/src/middleware/__init__.py`**  
   Add `from .require_auth import require_auth` so it remains importable as `from src.middleware import require_auth`.

3. **Update `_test_project/src/user_router.py`**  
   Change `from src.auth import require_auth, login` → `from src.auth import login` and add `from src.middleware import require_auth`.

4. **Keep `auth.py` clean**  
   Remove the `require_auth` function from `auth.py` (lines 94-107) along with the `import functools` and `from flask import request, jsonify` imports if they become unused (but keep `functools` if still used elsewhere, or remove and keep only needed imports).

### Verification

After approval and execution:
1. Run `python -c "from src.auth import auth_service, generate_token, verify_token, login; print('auth API OK')"` — backward compat.
2. Run `python -c "from src.middleware import require_auth; print('middleware import OK')"` — new import works.
3. Start the Flask app (`python src/app.py`) and hit `POST /api/users/login` → confirm auth still works.
4. Verify that `GET /api/users/` with/without valid token returns expected 200/401/403.
Now I have a full picture. Let me present the plan.

---

### Goal

Refactor `_test_project/src/auth.py` to follow a clean, testable, class-based design — replacing the current module-level global state and primitive functions with an `AuthService` class.

### Constraints

- Only modify files under `_test_project/`.
- Do **not** break existing callers: `user_router.py` imports `require_auth` and `login` from `src.auth`.
- Preserve the existing `SESSION_TABLE` schema in `src/models.py` — it already matches the data the module stores.
- All existing endpoint behavior must remain unchanged (login, token verification, auth decorator).

### Proposed Changes

**`_test_project/src/auth.py`** — Replace current code with:

1. **`AuthService` class** that encapsulates:
   - `_sessions` dict (instance-level, was the global `ACTIVE_SESSIONS`)
   - `_secret_key` and `_token_expiry_hours` as constructor params (injectable, no hard `from config import`)
   - `generate_token(user_id) -> str`
   - `verify_token(token) -> dict | None`
   - `login(username, password) -> str | None` (accepts a `UserService` instance or creates one)
   - `revoke_token(token) -> None` (new, for logout support)
   - `cleanup_expired() -> int` (new, removes expired tokens, fixes the memory leak)
   - `get_active_sessions() -> dict` (for introspection / admin)

2. **Module-level singleton** `auth_service = AuthService()` so existing imports like `from src.auth import require_auth, login` still work.

3. Keep the `require_auth` decorator at module level, calling the singleton's `verify_token`.

4. Keep `login` as a module-level function that delegates to the singleton.

**`_test_project/src/user_router.py`** (if needed):
- Adjust the `do_login` route or `delete_user` if the `AuthService` API changes in a way that affects them. Likely no change needed since we keep backward-compatible signatures.

### What this fixes

| Issue | How |
|---|---|
| Global mutable `ACTIVE_SESSIONS` | Now instance-level in `AuthService` |
| Hard-coded `from config import` | Injected via constructor |
| No token revocation | New `revoke_token()` method |
| Memory leak (sessions never cleaned) | `cleanup_expired()` + optional periodic call |
| Hard to unit-test | Constructor injection + no global state |
| No session-table alignment | Easier to later replace `_sessions` dict with DB via `SESSION_TABLE` schema |

### Verification

After execution (after plan approval):
1. Run `python -c "from src.auth import auth_service, require_auth, login; print('import OK')"` to verify backward compatibility.
2. Run any existing tests (if they exist) or manually smoke-test the login flow via `curl`/pytest of the app.
3. Confirm `require_auth` decorator still works on existing routes.
4. Confirm `auth_service.revoke_token(...)` and `auth_service.cleanup_expired()` work.
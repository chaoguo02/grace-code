"""P5: Hooks — synchronous lifecycle interceptors.

Hooks are registered with HookDispatcher and execute at specific
lifecycle points.  They can transform data, inject context, or
block operations (depending on the hook type's decision power).
"""

[PLAN MODE REMINDER] You are STILL in plan mode — read-only, no edits.
Use Agent subagents for exploration. Exit with ExitPlanMode when the plan is ready.
Do NOT make changes to files, run destructive commands, or modify the workspace.

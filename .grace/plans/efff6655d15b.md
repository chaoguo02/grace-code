## Phase 2: Design — Ready for Plan

Now I have all the information needed. The task is straightforward: add npm-based startup instructions to the README.

### Key findings:
- **Root `package.json`** at `D:\StudyProjects\ProjectBench\grace-code\package.json` has scripts: `start` (build:web + server), `dev:web` (cd web && npm run dev), `build:web` (cd web && npm run build), `server` (python -m server.main --repo .)
- **Web `package.json`** at `web/package.json` has scripts: `dev` (vite — hot reload), `build` (tsc && vite build), `preview` (vite preview)
- **Server** by default binds to `127.0.0.1:8765`, serves the built frontend from a static directory
- The README currently has **zero mention of npm** — only `pip install` and `python -m entry.cli` startup paths
- Users need to install web dependencies separately: `cd web && npm install`

### Design decision:
Add a new subsection **"5. Web 界面（npm 启动）"** after the current Chat mode section. This will cover:
1. Install Node.js dependencies (`cd web && npm install`)
2. Development mode (two terminals: backend + vite dev server with HMR)
3. Production/standalone mode (`npm start` from root)

---

## Final Plan

**File to modify:** `README.md`  
**Operation:** Insert a new "5. Web 界面（npm 启动）" section between line 74 (end of Chat mode table) and line 75 (`---` separator)

### Inserted content (new section):

```markdown
### 5. Web 界面（npm 启动）

项目包含一个 Vite + React 构建的 Web 前端，通过 npm 管理。

#### 安装 Node.js 依赖

```bash
# 在项目根目录安装 Node.js 依赖
cd web
npm install
```

#### 开发模式（热重载）

需要同时启动后端和前端两个进程：

```bash
# 终端 1：启动 Python 后端 API 服务（默认端口 8765）
python -m server.main --repo .

# 终端 2：启动 Vite 开发服务器（HMR 热重载）
cd web && npm run dev
```

前端开发服务器默认运行在 `http://localhost:5173`，API 请求会自动代理到后端。

#### 生产模式

构建前端产物并启动后端服务（由后端统一提供静态文件服务）：

```bash
# 构建前端 + 启动后端（一行命令）
npm start
```

或者分步执行：

```bash
# 先构建前端
npm run build:web

# 再启动后端（前端产物由后端 / 路由提供）
npm run server
```

访问 `http://localhost:8765` 即可使用 Web 界面。
```

### Verification:
After edit, the README will have `npm install`, `npm run dev:web`, `npm start` documented so users can start the project via npm.

---

```json
{
  "objective": "在 README.md 中添加 npm 启动本项目的说明文档",
  "execution_intent": "edit",
  "target_files": ["D:\\StudyProjects\\ProjectBench\\grace-code\\README.md"],
  "expected_behavior": "README.md 的「快速启动」章节新增第 5 小节「Web 界面（npm 启动）」，包含安装 Node.js 依赖、开发模式热重载、生产模式启动三种场景的命令说明",
  "verification_strategy": "手动检查 README.md 第 75 行附近是否插入了 npm 启动章节，且 `npm install`、`npm start`、`npm run dev:web` 命令均已列出",
  "potential_conflicts": ["README.md 第 75 行已有的 `---` 分隔符位置需要调整到新增内容之后"]
}
```